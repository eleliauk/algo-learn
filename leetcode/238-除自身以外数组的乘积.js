/**
 * @param {number[]} nums
 * @return {number[]}
 */
var productExceptSelf = function(nums) {
    var len=nums.length-1;
    var answer=[]
    for(let i=0;i<length;i++){
        answer.push()
    }
};